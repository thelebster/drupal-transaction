rules_transaction_execution:
  label: 'Transaction has been executed'
  category: 'Transaction'
  context_definitions:
    transaction:
      type: 'entity:transaction'
      label: 'The executed transaction'
